### Other changes

- Update the Python runtime to 3.14. The Docker base image is now `python:3.14.6-slim-bookworm`, and `.python-version`, `requires-python`, and the PyPI classifier are all bumped to match so that CI and the container run the same interpreter.
